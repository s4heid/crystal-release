#!/bin/bash

set -eux

echo "Testing runtime compability..."
# shellcheck disable=1091
source "/var/vcap/packages/<%= crystal_packagename %>/bosh/runtime.env"
crystal run /var/vcap/packages/<%= test_packagename %>/test.cr
